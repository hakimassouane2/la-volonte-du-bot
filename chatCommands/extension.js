function extension(client, channel) {
    client.say(channel, `​Notre extension est disponible tu peux y accèder sur le player vidéo Twitch ! Malheureusement Adblock peut entrer en conflit avec, donc n'hésites pas à le couper pour pouvour utiliser l'extension`);
}

export default extension