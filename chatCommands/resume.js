function resume(client, channel) {
    client.say(channel, `​📜 Pour avoir accès en exlusivité au wiki de l'aventure envoyez nous votre mail en DM pour recevoir l'invitation`);
}

export default resume